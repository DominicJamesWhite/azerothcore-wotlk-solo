# This is where you should add your custom scripts to be compiled.
# Note: you should use the name of the directory where the scripts are,
# instead of "Custom".
set(CUSTOM_SCRIPTS_SOURCES
    custom_script_loader.cpp
    spell_custom_bloomstrike.cpp
    spell_custom_empowered_touch.cpp
)
